#No xrange on Python3 (xrange displays value on demand)
#raw_input vs input
#print method
#Implicit relative imports should never be used and have been removed in Python 3.
#xrange, which is used for hughe range lists, it's leazy and creates at time, different than range which creates a list in memory
#some methods
#exceptions arguments syntax
